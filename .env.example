# ── Project Argus 環境設定範例 ──────────────────────────────
# 複製此檔為 .env 後填入你自己的金鑰。這些變數會被 Vite 打包進前端，
# 所以只放「可公開的展示層設定」，真正的機密（AI 推論、CDSE 帳密）
# 一律放在 Cloudflare Worker 的 secret 裡（見 cloudflare/README.md）。

# Sentinel Hub / Copernicus Data Space 的 WMS instance ID。
# 到 https://apps.sentinel-hub.com/dashboard 建立一個 configuration，
# 把 instance id 貼在這裡。沒填的話，影像圖層會顯示「未設定金鑰」提示，
# 而不是破圖。
VITE_SENTINEL_INSTANCE_ID=

# WMS 服務位址。CDSE (免費) 用下面這個；舊版 Sentinel Hub 商用版可自行替換。
VITE_SENTINEL_WMS_URL=https://sh.dataspace.copernicus.eu/ogc/wms

# 邊緣 AI 辨識代理（Cloudflare Worker）的網址。
# 沒填的話，前端會 fallback 到本機 mock 偵測，方便你先看 UI 流程。
VITE_EDGE_AI_URL=

# AIS 即時船舶資料金鑰（aisstream.io 免費申請）。
# 沒填的話，AIS 模式會顯示「模擬船隻」，方便先看效果。
VITE_AISSTREAM_KEY=

# 註：風場、洋流、落海漂流預判使用 Open-Meteo 免費開放 API，
#     不需要任何金鑰，開箱即可運作。
